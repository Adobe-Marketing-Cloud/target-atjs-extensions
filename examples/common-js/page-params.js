function targetPageParams(){
  return ['page=' + document.location.pathname, 'src=targetPageParams'];
}

function targetPageParamsAll(){
  return ['page=' + document.location.pathname, 'src=targetPageParamsAll'];
}
